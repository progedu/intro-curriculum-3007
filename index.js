'use strict';

/**
 * Check if the number is multiple of 17.
 * @param {number} num
 */
function isMultipleOfSeventeen(num) {
    return num % 17 === 0;
}

module.exports = { isMultipleOfSeventeen };
