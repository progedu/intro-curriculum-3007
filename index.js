'use strict';

/**
 * check if the number is multiple of seventeen
 * @param{num}
 */
function isMultipleOfSeventeen(num) {
    return num % 17 === 0;
}
module.exports = {
    isMultipleOfSeventeen
}
